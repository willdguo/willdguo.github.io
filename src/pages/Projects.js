import projects from '../data/projects';
export default function Projects() { return <div className="page-shell interior-page"><header className="page-heading"><p className="eyebrow">Projects / Notes</p><h1>Things I’ve explored.</h1><p>Course notes, software, and writing from the path to theoretical computer science.</p></header><section className="project-grid">{projects.map(project => <article className="project-card" key={project.title}><a className="project-image" href={project.link} target="_blank" rel="noreferrer"><img src={project.image} alt="" /></a><div className="project-meta"><span>{project.kind}</span><span>{project.date}</span></div><h2>{project.title}</h2><p>{project.description}</p><a className="text-link" href={project.link} target="_blank" rel="noreferrer">{project.linkLabel} <span aria-hidden="true">↗</span></a></article>)}</section></div>; }
